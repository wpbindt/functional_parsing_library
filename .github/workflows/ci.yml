name: tests

on:
  push:
    branches:
      - main
    tags:
      - '*'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Build image
      run: make image

    - name: Run tests
      run: make run-tests

    - name: Run mypy
      run: make mypy

  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref_type == 'tag'
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: pypi-AgEIcHlwaS5vcmcCJDEzZGUyNWQ1LThhZmUtNDljNy04NDBlLTQwNWQ3YzhmZjQ3OQACKlszLCI3NWVlMThkZi1iYWZkLTRkMGYtODMzNi1mZWVjNjQ0YmI1YjgiXQAABiCPXrUhOIzIRJzklzEHjfBTsdjseJvhyOuabfN91f41Fw
        run: |
          pip install twine
          make build
          python3 -m twine upload --verbose --non-interactive --repository pypi dist/*
